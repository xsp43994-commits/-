# `frozen_test_v1` 协议修订 001：取消功率敏感性实验

- 修订日期：2026-07-24（Asia/Shanghai）
- 原协议：`paper_runs/protocols/frozen_test_v1/protocol.json`
- 原协议 SHA-256：`7a010c749881dd452c78a03602d42c797b32b14590637f63676a216cd34dce16`
- 修订性质：用户明确提出的、在功率敏感性数据产生之前作出的范围缩减

## 修订内容

取消原计划在 `id_test` 上新增的功率倍率 `0.8、0.9、1.1、1.2`，不再执行：

- `power_core_learning` 的 6000 条学习模型评价；
- `power_main_baselines` 的 13200 条主基线评价；
- 合计 19200 条新增功率敏感性结果及其专属审计、聚合和曲线。

原冻结 `protocol.json` 不修改、不覆盖、不重新计算哈希。本文件作为可追溯修订记录解释原计划与最终执行范围之间的差异。

## 修订时点与数据状态

作出决定时，功率敏感性实验尚未启动；已核验：

- `power_core_learning*` 结果目录：0；
- `power_main_baselines*` 结果目录：0；
- 非 1.0 功率倍率正式结果：0 条。

因此本次修订没有删除、筛选或放弃任何已经观察到的功率敏感性结果，也不改变已经完成的训练、`id_test`、压力测试或后续规模泛化数据。

## 保留的实验范围

- 7 种学习方法、5 个训练种子和 105000 个训练回合；
- 正式 `id_test`：8000 条及严格审计；
- 压力测试：6800 条及严格审计；
- 同生成机制下的规模泛化：4800 条及严格审计；
- 主方法、消融和补充算法三个统计族；
- 统一 `MissionEvaluator`、主指标和全部辅助指标；
- 原算法预算、场景、检查点、种子和统计规则。

修订后的正式评价总量为 `8000 + 6800 + 4800 = 19600` 条，不含 validation 和开发验收数据。

## 论文主张与披露边界

本修订不影响 nominal power（`power_scale=1.0`）下的主方法比较、消融结论、压力工况结论和同生成机制规模泛化结论。

论文不得再主张：

- 对功率代理模型误差具有经过实验验证的鲁棒性；
- 在 `0.8–1.2` 功率倍率下保持稳定性能；
- 已完成真实功率遥测、功率模型校准或功率敏感性验证。

功率代理模型依赖应列为局限性，并将多倍率敏感性或真实功率测量列为未来工作。图表清单不再包含功率敏感性曲线。

## 取消原因

1. 修订发生在实验启动前，不损失已有数据；
2. 19200 条新增评价带来约两天的传统算法计算成本；
3. 功率敏感性不是本文 PPO＋Pointer、统一安全评价和消融比较的核心研究问题；
4. 保留 `id_test`、压力测试和规模泛化后，论文仍具有完整、可审计的主比较和二级验证链条。
