# v3.1.10：单约束非目标资源释放后备路径

- 被替代协议哈希：`86e19710e1422712f2412e05bfa19c07f7dfb19aab91e17fb68427c5fca98680`
- 新协议哈希：`59ba71e12cd9edb55390f4961c844e30b8277815523cb002b6a77c07b183c79c`
- 修订发生在任何试训和正式训练之前；没有运行或读取任何学习模型、贪心算法、元启发式算法或算法比较结果。
- 触发证据：
  - `validation__map_008__task_06`在680个候选后仍未完成；
  - 第500个候选的目标约束为时间，五轮目标资源校准把时间预算尺度从0.639放宽到1.590，覆盖上界仍低于0.60；
  - 即使把时间预算放宽到已注册上限2.2，覆盖上界仍约为0.53。
- 模型无关诊断：
  - 在保持能量预算不变时，仅把非目标航程尺度从1.170放宽到已注册上限2.2，MILP即可找到安全加权覆盖率0.714的返航路线；
  - 从原始候选释放两个非目标资源后，只需一轮目标时间校准，普通MILP证书为0.735，双阈值证书为`[0.714, 0.837]`；
  - 由此证明阻塞原因是非目标航程形成假性瓶颈，而不是求解器时限不足。
- 新增统一后备规则：
  - 仅适用于能量、航程或时间单约束任务，混合约束禁止使用；
  - 仅当原双阈值认证严格返回`low_threshold_no_route`时启用；
  - 从未经目标资源校准的原始候选重建；
  - 目标资源预算保持原值，另外两个非目标资源只放宽到协议中已注册的参数上限；
  - 随后仍只允许校准目标资源，最多五轮；
  - 最终仍必须通过相同双阈值MILP证书和目标瓶颈审计。
- 资源阈值后备证书从第200次起每100次候选触发一次，最晚到第1900次；这一固定周期对所有地图、节点规模、难度和单约束类型一致。
- 已有78条validation记录的地图、任务、预算、路线、MILP证书和文件字节均不改变；协议迁移只增加未完成任务的统一后备生成路径。
