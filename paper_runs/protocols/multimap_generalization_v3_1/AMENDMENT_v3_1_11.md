# v3.1.11：训练任务认证长尾提前处理与四路地图分片

- 被替代协议哈希：`59ba71e12cd9edb55390f4961c844e30b8277815523cb002b6a77c07b183c79c`
- 新协议哈希：`68fde73db57d07f4c9451ed54baa978e09985c5017c43e68adac54d6f2a5131a`
- 修订发生在任何试训和正式训练之前；没有读取任何学习模型或比较算法结果。
- 模型无关运行证据：
  - 首批42个training场景的候选次数中位数为0，90分位数为4，已接受场景最大为13；
  - `training__map_004__task_06`达到40次仍未产出，属于明显长尾；
  - 机器有16个逻辑核心，而串行认证进程CPU时间约等于墙钟时间，只利用约一个核心。
- 长尾规则修订：
  - 双阈值后备证书新增20、40、60、80、100次触发点；
  - 60秒低阈值和300秒高阈值证明标准不变；
  - 200次之后的原固定周期仍保留。
- 并行规则：
  - 剩余training地图按原注册表下标冻结为`[4,22)`、`[22,39)`、`[39,56)`、`[56,72)`四个分片；
  - 每个分片独立单进程、单线程运行，任务ID、map_index、随机种子和MILP规则与串行实现相同；
  - 已有42条串行记录保留为合并输入；
  - 分片与已有记录发生重复时，完整任务对象必须逐字段一致；
  - 合并必须覆盖72张地图×9个任务的648个唯一任务，并通过全量证书审计后才能封存。
- 8张真实DEM、84张程序化训练/validation地图、108条validation记录和42条training记录的文件内容均不改变。
