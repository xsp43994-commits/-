# v3.1.15 混合阈值短缺门禁修订

- 修订发生在任何试训或正式训练之前；四个training分片在229个唯一任务时暂停。
- v3.1.14回放证明：标准MILP下界距离目标带较远的候选，即使依次运行三种60秒
  下阈值MILP，也可能没有可用路线，造成单候选数分钟的无效计算。
- 下阈值救援现仅用于“标准可行解距离散下界最多差1个优先级权重单位”的候选。
- 已经位于难度带内但缺少第二瓶颈的标准路线仍走零额外阈值求解的快速路径。
- 距离难度带更远的候选直接拒绝并继续下一个确定性候选；不改变任何场景接受标准。
- 229个既有training唯一任务、108个validation任务及全部地图内容必须逐字节保留。
