# v3.1.9：单约束双阈值资源MILP后备证书

- 被替代协议哈希：`a5010fff297142de5bf3b5e889df38b6598de61cb25c0686e3083396c5ad9e40`
- 新协议哈希：`86e19710e1422712f2412e05bfa19c07f7dfb19aab91e17fb68427c5fca98680`
- 修订发生在任何试训和正式训练之前；没有运行或读取任何学习模型、贪心算法或元启发式算法结果。
- 触发证据：`validation__map_007__task_06`在200个候选中出现198次10秒MILP证书缺失，继续盲目搜索至2000次没有合理收益。
- 模型无关诊断：
  - 覆盖率至少0.70的离散阈值为35/49；找到的安全路线最短航程候选为9906.84 m，低于原预算10400 m。
  - 首个高于0.85的离散阈值为42/49；严格最短航程为11762.31 m，高于原预算10400 m。
  - 因而该候选的认证覆盖区间为 `[35/49, 41/49] = [0.7143, 0.8367]`，完整落入中等难度区间。
- 新增统一后备规则：
  - 仅适用于能量、航程或时间单约束任务，混合约束禁止使用；
  - 只在候选尝试次数200、500、1000触发；
  - 低阈值MILP限时60秒，只负责提供在原预算下可安全执行的覆盖下界路线；
  - 高阈值最小资源MILP限时300秒；只有求解器证明不可行，或其严格dual下界超过原目标资源预算时，才允许形成覆盖上界；
  - 两个阈值由难度区间和整数优先级权重自动计算，不按地图或算法结果设置。
- 增加未完成任务的精确断点恢复；恢复时从检查点记录的候选编号继续，不再从第0个候选重复搜索。
- 已有69条validation记录的地图、任务、预算、路线和MILP证书均不改变；协议迁移只增加后备认证能力和审计规则。
