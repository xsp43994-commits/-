# v3.1.17：Windows 原子状态写入恢复性修订

触发条件：v3.1.16 的首个正式模型在第724回合停止。训练指标有限、500回合健康报告未触发集体停止、未完成任何正式模型；错误为 Windows `WinError 5` 在替换 `status.json` 时的瞬时文件占用。

修订仅为 `paper_multimap_experiments.py` 的原子替换增加7次、初始50 ms指数退避的重试，且仅接受 WinError 5、32、33。它不改变地图、任务记录、DEM、奖励、网络、掩码、优化器、训练回合、评价器或选择规则。

处理方式：保留并归档v3.1.16的环境封存、失败断点、日志和试训原始判定；全部场景记录逐字节验证不变；重新封存v3.1.17环境，并从零启动尚未产生正式结果的35模型训练批次。
